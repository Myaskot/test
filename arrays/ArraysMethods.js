'use strict'
// methods
var a = [1, 2, 3]

// push
// a.push(10)

// pop
// a.pop()

// unshift
// a.unshift(2)

//shift
// a.shift()
// console.log(a)

var b = [1, 2, 3, 4] // map, filter, reduce

// var mapped = b.map(function (element, index) {
//   return element + index;
// });
// console.log(mapped)

// var filtered = b.filter(function (element) {
//   var result = (element > 2);
//   return result;
// })
// console.log(filtered); // 3, 4

// var reduced = b.reduce(function (acc, element, index) {
//   return acc + element;
// });
// console.log(reduced); // 10
